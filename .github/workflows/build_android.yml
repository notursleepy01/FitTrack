name: Build FitTrack Android APK

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Java
        uses: actions/setup-java@v3
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'

      - name: Install dependencies
        run: flutter pub get
      
      - name: Recreate platform-specific files
        run: flutter create --org dev.sleepy --platforms=android .
      
      - name: Fix NDK version in build.gradle
        run: |
          sed -i '/android {/a \ \ \ \ ndkVersion = "27.0.12077973"' android/app/build.gradle.kts

      - name: Generate launcher icons
        run: flutter pub run flutter_launcher_icons

      - name: Run Hive generator
        run: flutter pub run build_runner build --delete-conflicting-outputs
      
      # --- THIS IS THE KEY CHANGE ---
      - name: Build Universal APK
        run: flutter build apk

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: fittrack-universal-apk
          path: build/app/outputs/flutter-apk/app-release.apk # The universal build command still produces a file named "app-release.apk"
